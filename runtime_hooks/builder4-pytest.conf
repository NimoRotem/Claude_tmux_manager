# Recreate builder4's immutable pytest serialization lock after every boot.
d /run/lock/builder4 0750 root nimrod_rotem -
f /run/lock/builder4/pytest-heavy.lock 0440 root nimrod_rotem -
z /run/lock/builder4/pytest-heavy.lock 0440 root nimrod_rotem -
