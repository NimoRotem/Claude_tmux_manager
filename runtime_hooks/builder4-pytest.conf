# Recreate builder4's immutable pytest serialization lock after every boot.
d /run/lock/builder4 0700 root root -
f /run/lock/builder4/pytest-heavy.lock 0400 root root -
z /run/lock/builder4/pytest-heavy.lock 0400 root root -
