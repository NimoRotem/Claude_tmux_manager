  the browser policy.

  Auto-push is not a bug. "basic" clears numbered option menus and never types
  free text, so it can never clear a question, and builder6 runs the same
  setting. I confirmed "full" would have typed, by running the live detector
  against the real stuck pane. Left on basic: full was demoted fleet-wide
  deliberately, and a local app.py edit is overwritten by the next upgrade from
  builder6.

  Check any new box with wc -c ~/AGENTS.md. Around 600 bytes means it never
  pulled. Your builder5 tenant is fine.

  Left undone: parity reports builder7 has no gh auth, no github ssh key, and
  zero API keys, skills and infra sidecars against peers. Its advisor token is
  still builder6's. That session's work completed, so it is safe to dismiss.

✻ Cooked for 10m 38s · done 6:34 PM

────────────────────────────────────────────────────────────────────────────────
❯ 
────────────────────────────────────────────────────────────────────────────────
  ⏵⏵ bypass permissions on (shift+tab to cycle) · PR #93 · ← for agents
                                         ✔ Update installed · Restart to apply

